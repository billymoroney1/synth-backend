const db = require('../models/index')
const { populate } = require('../models/user.model')

//access to users and presets
const User = db.user
const Post = db.post

//make a preset

//edit preset

//delete preset

//get presets